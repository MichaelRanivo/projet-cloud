apiVersion: v1
kind: Service
metadata:
  name: sample-app-service-clusterip
  namespace: sample-app-kub-namespace
spec:
  type: ClusterIP
  selector:
    app: sample-app
  ports:
  - port: 80
    targetPort: 8080
